// Deploy-time backend URL overrides. Edit this file at deploy time;
// the dashboard loads it before booting so the connect form can use it.
window.__METACUBEXD_CONFIG__ = {
  defaultBackendURL: "",
  // controlToken: '', // All-in-One server mode: injected by the server
};
